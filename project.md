P1 — flat procedural physics
   one streamtube, one blade row, 7 equations, hard-coded inputs
   goal: a number you trust

P2 — identify natural boundaries
   let messy flat code reveal what wants to become an object
   introduce only what the physics demands

P3 — minimal objects
   FlowStation, BladeRow emerge from P2 friction
   no Socket/Subelement pattern yet

P4 — multi-streamtube
   extend to N streamtubes once single tube is solid
   Expander/Reducer logic introduced here